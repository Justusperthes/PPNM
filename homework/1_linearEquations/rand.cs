public static class Rand(){
	var rnd = new System.Random(1); /* or any other seed */
	double x = rnd.NextDouble();
	double y = rnd.NextDouble();
	double z = rnd.NextDouble();
	public static var getRnd(){
		return getRnd;
	}
}

using System;
using static System.Console;

class main{
	static double x = 1;
	int i;
	string hello = $"hello, x={x}\n";
	double times2(double y){
		double z=7;
		{
			double x=9;
		}
		WriteLine(x);
		return y*2;};
		}
	static int Main(){
		Write($"x={x} Sin(x)={Sin(x)}\n");
		for(double x=1;x<10;x+=1){
			Write($"fgamma({x})={sfuns.fgamma(x)} {x}!={prod}\n");
			prod*=x;
			}
		return 0;
	}
}
